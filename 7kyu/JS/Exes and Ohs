function XO(str) {
  var xCount = 0;
  var oCount = 0;
  for (let i = 0; i<str.length; i++){
    if(str[i].toLowerCase() == "x"){
      xCount++;
    }else if(str[i].toLowerCase() == "o"){
      oCount++;
    }
  }
  return xCount == oCount;
}
